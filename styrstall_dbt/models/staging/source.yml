version: 2

sources:
  - name: raw
    schema: main_raw
    database: dev
    tables:
      - name: raw_stations
        meta:
          dagster:
            asset_key: ["raw_stations"]
      - name: raw_weather
        meta:
          dagster:
            asset_key: ["raw_weather"]


